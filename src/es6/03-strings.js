let hello = 'Hello'
let world = 'World'
let epicPhrase = hello + ' ' + world + '!';
console.log(epicPhrase)

// Templete literals
let mostAmazingPhrase = `${hello} ${world}! `
console.log(mostAmazingPhrase)

